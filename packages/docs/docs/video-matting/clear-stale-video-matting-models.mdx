---
image: /generated/articles-docs-video-matting-clear-stale-video-matting-models.png
title: clearStaleVideoMattingModels()
crumb: '@remotion/video-matting'
---

# clearStaleVideoMattingModels()<AvailableFrom v="4.0.522" />

Removes cached model files that are no longer returned by [`getAvailableVideoMattingModels()`](/docs/video-matting/get-available-video-matting-models).

There are no stale models in the current version, so the function resolves without changing the cache.

Call this function after upgrading `@remotion/video-matting` to remove files for models discontinued by a newer version.

```ts twoslash title="clear-stale-models.ts"
import {clearStaleVideoMattingModels} from '@remotion/video-matting';

await clearStaleVideoMattingModels();
```

## Return value

A `Promise<void>` that resolves after stale model files have been removed.

## Compatibility

<CompatibilityTable chrome firefox safari nodejs="" bun="" serverlessFunctions="" clientSideRendering={false} serverSideRendering player studio hideServers />

## See also

- [`getAvailableVideoMattingModels()`](/docs/video-matting/get-available-video-matting-models)
- [`isVideoMattingModelCached()`](/docs/video-matting/is-video-matting-model-cached)
